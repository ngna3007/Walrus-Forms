// Re-export hub kept for any future markdown utilities.
// Rendering happens via the React component in `components/MarkdownView.tsx`,
// which uses `react-markdown` + `rehype-sanitize` (no raw HTML injection).
export {};
